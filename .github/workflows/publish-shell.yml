name: Publish Shell to Cloudflare Pages

# Fast shell-only deploy to Cloudflare Pages (#362 follow-up). Fires on every
# push to staging or main that touches user-facing files (HTML/CSS/JS/manifest)
# so the CF Pages site is ALWAYS current without waiting on the full pipeline
# in update-data.yml. Data (per-game buckets under data/) is untouched -- that
# still lives in R2 and is refreshed by the pipeline on its own cadence.
#
# Deploy target selection:
#   staging branch -> Cloudflare Pages project "proton-pulse-web-staging"
#                     (custom domain: staging.proton-pulse.com)
#   main    branch -> Cloudflare Pages project "proton-pulse-web"
#                     (custom domain: www.proton-pulse.com)
#
# Path filter: only trigger when the shell actually changes. Skips pipeline-
# only edits (scripts/, supabase/, workflow YAML for other jobs) so we do not
# burn a build minute on a change that has no user-visible effect. Manual
# dispatch is always available.

on:
  push:
    branches: [staging, main]
    paths:
      - '*.html'
      - 'css/**'
      - 'js/**'
      - 'gh-pages-manifest.txt'
      - 'scripts/publish-cloudflare.sh'
      - 'scripts/preserve-cert-monitor.sh'
      - '.github/workflows/publish-shell.yml'
  workflow_dispatch: {}

concurrency:
  # Shared group with update-data.yml's finalize job so shell publish and
  # full pipeline can never both deploy to the SAME CF Pages project at the
  # same time. Group keys on the destination project (not github.ref_name)
  # because a manual dispatch of the pipeline on main with
  # staging_with_finalize=true still deploys to the staging project -- we
  # want that to serialize with a push to staging too.
  #
  # cancel-in-progress: false = queue rather than kill. If a long pipeline
  # is running, a shell push waits behind it (~ minutes to ~ hour) rather
  # than losing the pipeline work. The version-check inside
  # publish-cloudflare.sh is the second safety net: it refuses to deploy
  # an older commit on top of a newer one, so even if a stale pipeline
  # finishes last it will not overwrite a fresh shell push.
  group: cf-pages-${{ github.ref_name == 'staging' && 'proton-pulse-web-staging' || 'proton-pulse-web' }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          # Full history so publish-cloudflare.sh can compute VERSION + SHA
          # and preserve-cert-monitor.sh can git-show from origin/gh-pages.
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          # Wrangler 4.x requires >=22 -- match .github/workflows/deploy-worker.yml.
          node-version: '22'

      - name: Resolve deploy target
        id: target
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          # staging.proton-pulse.com is served by the "-staging" CF project;
          # www.proton-pulse.com by the plain one. PAGES_BRANCH mirrors so the
          # deploy shows up under the right named preview environment in the
          # CF Pages dashboard.
          if [ "$REF_NAME" = "staging" ]; then
            echo "project=proton-pulse-web-staging" >> "$GITHUB_OUTPUT"
            echo "branch=staging" >> "$GITHUB_OUTPUT"
          else
            echo "project=proton-pulse-web" >> "$GITHUB_OUTPUT"
            echo "branch=main" >> "$GITHUB_OUTPUT"
          fi

      - name: Preserve top-level data files from the live target site
        # publish-cloudflare.sh's SMALL_DATA list expects search-index.json,
        # most_played.json, etc. to exist in OUTPUT_DIR. On a shell-only push
        # there is no fresh pipeline output, so re-download whatever the LIVE
        # TARGET currently serves and redeploy it unchanged.
        #
        # It must be the live target, NOT gh-pages: gh-pages holds PROD's
        # last pipeline output. Pulling it while deploying the STAGING shell
        # silently regressed staging's data to prod state on every push --
        # staging's pipeline had merged 4,524 pgwiki: search-index rows and
        # one shell push wiped them. Env-specific data stays env-specific.
        #
        # A file the live site lacks (or serves as the SPA HTML fallback --
        # CF Pages returns 200 text/html for unknown paths) is skipped, and
        # publish-cloudflare.sh's optional-copy loop tolerates the gap.
        env:
          LIVE_BASE: ${{ steps.target.outputs.project == 'proton-pulse-web-staging' && 'https://staging.proton-pulse.com' || 'https://www.proton-pulse.com' }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/shell-out
          FILES=(
            search-index.json search-index-steam-extended.json most_played.json
            recent-reports.json stats.json coverage-summary.json data-versions.json
            game-images.json game-images-skip.json nonsteam-images.json nonsteam-metadata.json
            deck-status.json proton-versions.json steam-catalog.json
            hardware-suggestions.json scoring-info.json form-schema.json
            app-id-redirects.json pcgamingwiki.json pcgwiki-catalog.json
            pcgw-id-map.json flightless-benchmarks.json flightless-review-queue.json
            sgdb-covers.json
            # This list is a FOURTH place a pipeline data file must be
            # registered (after gh-pages-manifest.txt, the update-data.yml
            # copy loops, and publish-cloudflare.sh SMALL_DATA). Miss it and
            # the file survives a pipeline run but is dropped by the next
            # shell deploy, so the site works until someone pushes a CSS
            # tweak. vr-index.json + vrdb.json (#246) and anti-cheat.json
            # were all lost this way.
            vr-index.json vrdb.json anti-cheat.json
          )
          for f in "${FILES[@]}"; do
            if curl -sfL --max-time 30 "$LIVE_BASE/$f" -o "/tmp/shell-out/$f" \
               && [ -s "/tmp/shell-out/$f" ] \
               && head -c 1 "/tmp/shell-out/$f" | grep -qv '<'; then
              echo "preserved $f from $LIVE_BASE"
            else
              rm -f "/tmp/shell-out/$f"
              echo "skipping $f (absent on live site or HTML fallback)"
            fi
          done
          # coverage.html + data-index.html are the two pipeline-rendered HTML
          # pages linked from the Browse nav (#447). They obviously start with
          # '<' so the JSON preservation loop above rejects them. Fetch each
          # separately and keep only if the <title> is NOT the marketing
          # homepage fallback, which is how CF Pages responds when the file
          # is missing. Without this the nav items land on the SPA fallback
          # instead of the real report.
          HTML_FILES=(coverage.html data-index.html)
          for f in "${HTML_FILES[@]}"; do
            if curl -sfL --max-time 30 "$LIVE_BASE/$f" -o "/tmp/shell-out/$f" \
               && [ -s "/tmp/shell-out/$f" ] \
               && ! grep -q '<title>Proton Pulse | Smart Proton Launch Configurations' "/tmp/shell-out/$f"; then
              echo "preserved $f from $LIVE_BASE"
            else
              rm -f "/tmp/shell-out/$f"
              echo "skipping $f (absent on live site or SPA marketing fallback)"
            fi
          done
          # Plugin-repo-sourced files that the FULL pipeline curls from
          # decky-proton-pulse. They may be absent on a fresh staging site
          # that has never run a full pipeline. Fetch them as a fallback so
          # the submit page and scoring tooltips never 404 on a shell push.
          for pf in scoring-info.json form-schema.json; do
            if [ ! -s "/tmp/shell-out/$pf" ]; then
              if curl -sfL --max-time 15 \
                "https://raw.githubusercontent.com/mdeguzis/decky-proton-pulse/main/src/data/$pf" \
                -o "/tmp/shell-out/$pf" && [ -s "/tmp/shell-out/$pf" ]; then
                echo "fetched $pf from plugin repo (live-target copy absent)"
              else
                rm -f "/tmp/shell-out/$pf"
              fi
            fi
          done

      - name: Deploy shell to Cloudflare Pages
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # SKIP_R2_SYNC=1 is the "shell only" mode -- the per-game data/
          # buckets are already in R2 from the last pipeline run and do not
          # need re-syncing on every code push. Wall time drops from ~30 min
          # (with R2 sync) to ~1-2 min.
          SKIP_R2_SYNC: '1'
          PAGES_PROJECT: ${{ steps.target.outputs.project }}
          PAGES_BRANCH: ${{ steps.target.outputs.branch }}
        run: |
          bash scripts/publish-cloudflare.sh /tmp/shell-out "$GITHUB_WORKSPACE"
